import 'package:flutter/material.dart';

class LinhaHorizontal extends StatelessWidget {
  const LinhaHorizontal({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(color: Color(0xFFD8DB74), thickness: 2);
  }
}
